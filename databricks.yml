bundle:
  name: python_package

include:
  - resources/*.yml

artifacts:
  default:
    type: whl
    path: .

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-2836125740739785.5.azuredatabricks.net
  prod:
    mode: production
    workspace:
      host: https://adb-2836125740739785.5.azuredatabricks.net
      root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    run_as:
      service_principal_name: 6905bbb3-f6ee-4923-a7be-6cdcb0c254b2